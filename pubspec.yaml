name: pdfium_bindings
description: This project aims to wrap the complete [Pdfium](https://pdfium.googlesource.com/pdfium/) API in dart, over FFI.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  ffi: ^1.1.2  

dev_dependencies: 
  path: ^1.8.1 
  build_runner: ^2.1.2
  ffigen: any #^2.4.2
  image: any
#dart run ffigen --config ffigen.yaml